*o 親を渡したら確率的に二値を返す
*o 任意の長さのリストから確率的に二値を生成した結果を返す
*o 親を指定したら一つの二分木を作れる
*o 親のリスト 深さ，（生成する木の個数），確率 を指定したら 木の個数xP(= 2^(D-1)?) の行列を得られる
   - 生成する木の個数は親のリストから分かる．
*o 親のリストを渡したら，例題の答えとして扱える形の行列を得られる
   - 入力（identity()）と出力のタプルにする？今は出力のみ
